mediapipe>=0.10.0
opencv-contrib-python>=4.8.0
numpy>=1.24.0
# Windows reports 64-bit Intel/AMD machines as "AMD64", not "x86_64".
# Select by OS so TensorFlow is not silently skipped on Windows.
tensorflow-cpu>=2.13.0; platform_system == "Windows" or platform_system == "Linux"
tensorflow>=2.13.0;     platform_system == "Darwin"
Pillow>=10.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
customtkinter>=5.2.0
