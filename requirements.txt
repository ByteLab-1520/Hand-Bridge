mediapipe>=0.10.0
opencv-python>=4.8.0
numpy>=1.24.0
# Apple Silicon(arm64): tensorflow-cpu 없음 → tensorflow 사용 (config.py가 Metal 비활성화)
# 그 외 x86_64 (Windows / Linux / Intel Mac): tensorflow-cpu 사용
tensorflow-cpu>=2.13.0; platform_machine == "x86_64"
tensorflow>=2.13.0;     platform_machine == "arm64"
Pillow>=10.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
customtkinter>=5.2.0
